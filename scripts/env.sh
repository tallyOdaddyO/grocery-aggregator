# Source this to put the project's toolchain on PATH:  . scripts/env.sh
#
# Node was installed under $HOME (no sudo, no changes to your shell profile).
# Add the export below to ~/.zshrc yourself if you want it permanently.
export PATH="$HOME/.local/node/bin:$PATH"
